spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fhir_mii
    username: fhir
    password: fhir
    driver-class-name: org.postgresql.Driver
  sql:
    init:
      mode: always

app:
  cors:
    allowed-origins: http://localhost:5500,http://127.0.0.1:5500,https://alekseidudchenko.github.io

management:
  endpoints:
    web:
      exposure:
        include: health

server:
  port: 8080
